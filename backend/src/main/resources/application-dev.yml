# dev 프로파일: 로컬 개발 편의 설정
spring:
  jpa:
    hibernate:
      # validate 대신 update — 로컬에서 스키마 자동 동기화
      # 운영/스테이징에서는 반드시 validate + Flyway/Liquibase 사용
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
